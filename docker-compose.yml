services:
  web:
    build:
      context: .
      target: build
    command: npm run dev
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "8080:5173"
    restart: unless-stopped
